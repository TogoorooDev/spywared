#include <Windows.h>

#include "write.h"

void capture(){
    HWND winHWND = GetForegroundWindow();
    // PrintWindow(winHWND, )

}

int genXLen(LPRECT rect){
    int len;
    
    len = 0;
    return len;
}

int getYLen(LPRECT rect){
    int len;



    len = 0;
    return 0;
}